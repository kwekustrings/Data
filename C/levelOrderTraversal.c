#include <stdio.h>
#include <stdlib.h>

struct node
{
    char data;
    struct node *left, *right;
};

void levelOrder(struct node* root)
{
    if (root == NULL) return;

    //For function to work create a queue of pointer to node
    
}

/**********
int main(void)

*****/